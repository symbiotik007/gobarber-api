[build]
builder = "nixpacks"

[deploy]
startCommand = "node -r sucrase/register src/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
